export const PROCESS_EXPORT = 'PROCESS_EXPORT'

export default {
	[PROCESS_EXPORT] (state, response) {
        // let res = response.data;
        // if (res.status == 200) {
        //     state.excelLink = res.data;
        // }
    }
}